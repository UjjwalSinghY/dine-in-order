spring:
  application:
    name: dine-in-order-api
#DataDase configuration
  datasource:
    url: jdbc:mysql://localhost:3306/dine_in_order?createDataBaseIfNotExist=true
    username: root
    password: tiger
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update